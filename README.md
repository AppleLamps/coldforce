# coldforce archive

Static searchable archive for `@coldforce.bsky.social`.

## Local preview

```sh
python -m http.server 8000
```

Open `http://localhost:8000`.

## Deploy

This is a static site. Import the repository in Vercel and deploy with the default static settings.
